__pycache__/
*.pyc
obj/
obj_*/
*.dll
*.exe
*.obj
*.exp
*.lib
*.pdb
*.log
dist/
*-plan.md

# Upstream AGPL-3.0 headers (YYToolkit, Aurie, FunctionWrapper): placed by
# tools/prepare_toolchain.py, never committed.
plugin_build/include/

# decompiler artifacts never enter the repository (hub AGENTS.md, Legal)
*.gpr
*.i64
*.idb
Export/

# Live verification records (reports, receipts, spool samples, Vault copies,
# save backups) stay on the machine that made them: they hold local paths and
# the tester's characters and items. The public repository keeps none of them.
verification/
